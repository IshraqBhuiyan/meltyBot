#include "Arduino.h"
#include "accel.h"

//#include "controls.h"
//#include "teleop.h"
//#include "auton.h"
//#include "parseRadio.h"
//#include "acclerometer.h"


void setup() {
  // put your setup code here, to run once:
  //robotSetup();

}

void loop() {
  /*
  if(getKill()){
    //exit(0);
  }
  else if(!getRadioInput()){
    //autonMethod();
  }else{
    //radioControl();
  }
  // put your main code here, to run repeatedly:
  */
}
